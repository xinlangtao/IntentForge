# intentforge-space

Space isolation aggregation module for hierarchical company/project/product/application configuration and
multi-resource bindings across `skill/agent/prompt/tool/model/model-provider/memory/config`.
Includes `intentforge-space-core` for contracts and models, plus `intentforge-space-local` for local runtime implementations.
